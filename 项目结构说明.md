# 项目结构说明

> Vercel部署版本 - 完整项目结构

---

## 📁 目录结构

```
完整应用/
│
├── api/                          # Vercel Serverless Functions
│   ├── prototype/               # 原型生成API
│   │   ├── generate.js         # POST /api/prototype/generate
│   │   └── [id].js             # GET /api/prototype/:id
│   │
│   ├── data/                    # 数据分析API
│   │   ├── analyze.js          # POST /api/data/analyze
│   │   └── [id].js             # GET /api/data/:id
│   │
│   ├── code/                     # 代码审查API
│   │   ├── review.js           # POST /api/code/review
│   │   └── [id].js             # GET /api/code/:id
│   │
│   ├── design/                   # 设计生成API
│   │   ├── generate.js         # POST /api/design/generate
│   │   └── [id].js             # GET /api/design/:id
│   │
│   ├── _utils/                   # 共享工具函数
│   │   ├── aiService.js        # AI服务（Claude API）
│   │   ├── storage.js          # 存储服务（内存存储）
│   │   └── parseForm.js        # 表单解析（可选）
│   │
│   ├── projects.js              # GET /api/projects
│   └── health.js                # GET /api/health
│
├── public/                       # 前端静态文件（Vercel自动服务）
│   ├── index.html              # 主页面
│   ├── css/
│   │   └── styles.css          # 样式文件
│   └── js/
│       ├── api.js              # API调用封装
│       └── app.js              # 主应用逻辑
│
├── .github/                      # GitHub配置
│   └── workflows/              # GitHub Actions
│       ├── deploy.yml          # 自动部署
│       └── ci.yml             # CI检查
│
├── vercel.json                  # Vercel配置文件
├── package.json                 # 依赖管理
├── .env.example                 # 环境变量示例
├── .gitignore                   # Git忽略文件
│
└── 文档/
    ├── README.md               # 项目说明
    ├── VERCEL部署指南.md       # 部署指南
    ├── 功能结构检查清单.md     # 功能检查
    ├── 部署检查清单.md         # 部署检查
    └── 项目结构说明.md         # 本文档
```

---

## 🔧 关键文件说明

### API路由文件

#### `api/prototype/generate.js`
- **功能**：生成原型
- **方法**：POST
- **输入**：`{ requirement, title }`
- **输出**：`{ success, projectId, message }`

#### `api/data/analyze.js`
- **功能**：分析Excel数据
- **方法**：POST
- **输入**：`{ files: [{ data: base64, name, ... }], title }`
- **输出**：`{ success, projectId, message }`

#### `api/code/review.js`
- **功能**：审查代码
- **方法**：POST
- **输入**：`{ code, title }`
- **输出**：`{ success, projectId, message }`

#### `api/design/generate.js`
- **功能**：生成设计稿
- **方法**：POST
- **输入**：`{ requirement, title, brandSettings }`
- **输出**：`{ success, projectId, message }`

### 工具函数

#### `api/_utils/aiService.js`
- **功能**：封装Claude API调用
- **方法**：
  - `generatePrototype(requirement)`
  - `analyzeData(fileBuffer, filename)`
  - `reviewCode(code)`
  - `generateDesign(requirement, brandSettings)`

#### `api/_utils/storage.js`
- **功能**：数据存储服务（内存存储）
- **方法**：
  - `createProject(type, title, inputData)`
  - `updateProject(id, outputData, status)`
  - `getProject(id)`
  - `getProjects(type, limit)`

### 前端文件

#### `public/index.html`
- **功能**：主页面
- **结构**：导航栏 + 侧边栏 + 主内容区

#### `public/js/api.js`
- **功能**：API调用封装
- **特点**：使用相对路径 `/api/...`

#### `public/js/app.js`
- **功能**：主应用逻辑
- **功能**：模块切换、文件上传、状态管理

---

## 🚀 Vercel部署特性

### Serverless Functions
- 所有API路由在 `api/` 目录下
- 每个文件导出一个默认函数
- 自动处理路由和参数

### 静态文件
- `public/` 目录自动服务静态文件
- 无需额外配置

### 环境变量
- 在Vercel Dashboard中配置
- 自动注入到Serverless Functions

---

## 📝 注意事项

1. **存储方案**：当前使用内存存储，重启会丢失数据
2. **文件上传**：使用Base64编码，有大小限制
3. **API超时**：默认60秒，可在vercel.json中调整
4. **CORS**：已配置允许所有来源，生产环境建议限制

---

*项目结构已优化，适配Vercel部署。*
