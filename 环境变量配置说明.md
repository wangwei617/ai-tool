# 环境变量配置说明

> OpenRouter API配置指南

---

## 🔑 API密钥配置

### 方式1：使用OpenRouter API（推荐）

**优势**：
- ✅ 支持多个AI模型（Claude、GPT、Gemini等）
- ✅ 统一的API接口
- ✅ 灵活的模型切换
- ✅ 成本优化

**配置步骤**：

1. **获取OpenRouter API密钥**
   - 访问 [OpenRouter](https://openrouter.ai/)
   - 注册/登录账号
   - 创建API密钥
   - 或使用你提供的密钥：`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`

2. **配置环境变量**
   - 在Vercel Dashboard中，添加环境变量：
     ```
     OPENROUTER_API_KEY=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
     ```

3. **配置应用URL**（可选）
   ```
   APP_URL=https://your-app.vercel.app
   ```

### 方式2：使用Anthropic Claude API（备选）

如果需要直接使用Claude API：

1. 获取Anthropic API密钥
2. 配置环境变量：
   ```
   ANTHROPIC_API_KEY=your_anthropic_api_key
   ```
3. 修改 `api/_utils/aiService.js` 使用Anthropic SDK

---

## 📝 环境变量列表

| 变量名 | 说明 | 必需 | 默认值 |
|--------|------|------|--------|
| `OPENROUTER_API_KEY` | OpenRouter API密钥 | ✅ 是 | - |
| `APP_URL` | 应用URL（用于统计） | ⚪ 否 | - |
| `ANTHROPIC_API_KEY` | Anthropic API密钥（备选） | ⚪ 否 | - |

---

## 🎯 支持的AI模型

使用OpenRouter可以访问以下模型：

### Claude模型
- `anthropic/claude-3.5-sonnet`（默认）
- `anthropic/claude-3-opus`
- `anthropic/claude-3-haiku`

### GPT模型
- `openai/gpt-4-turbo`
- `openai/gpt-4`
- `openai/gpt-3.5-turbo`

### 其他模型
- `google/gemini-pro`
- `meta-llama/llama-3-70b-instruct`
- 更多模型请查看 [OpenRouter Models](https://openrouter.ai/models)

---

## 🔧 修改默认模型

在 `api/_utils/aiService.js` 中修改：

```javascript
// 修改默认模型
const DEFAULT_MODEL = 'openai/gpt-4-turbo'; // 或其他模型
```

---

## ⚠️ 注意事项

1. **API密钥安全**
   - ✅ 不要提交API密钥到Git
   - ✅ 在Vercel Dashboard中配置
   - ✅ 使用环境变量管理

2. **API调用成本**
   - ⚠️ 不同模型价格不同
   - ⚠️ 注意API调用费用
   - 💡 建议设置使用限额

3. **模型选择**
   - 💡 Claude 3.5 Sonnet：适合复杂任务
   - 💡 GPT-4 Turbo：速度快，成本适中
   - 💡 Claude 3 Haiku：成本低，速度快

---

## ✅ 配置验证

部署后，访问 `/api/health` 检查配置是否正确。

---

*配置完成后，应用即可使用OpenRouter API调用AI模型！*
